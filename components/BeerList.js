import styles from "../styles/components.module.scss";

const BeerList = ({ beerList }) => {
  return (
    <>
      {/* {beerList.list.map((item, index) => {
        <p key={index}>{item.name}</p>;
      })} */}
    </>
  );
};

export default BeerList;
